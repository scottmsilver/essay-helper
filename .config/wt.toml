# Worktrunk project hooks for essay-helper

# Install dependencies when creating a new worktree
post-create = "npm install --ignore-scripts && npx husky || true"

# Start the Vite dev server in the background after switching
post-start = "npm run dev"
